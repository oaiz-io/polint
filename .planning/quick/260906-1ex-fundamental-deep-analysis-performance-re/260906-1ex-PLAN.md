---
type: quick
status: complete-with-limitations
description: Fundamental deep-analysis performance research with pinned corpus measurements and unchanged accuracy
---

# Deep-analysis performance research

Work only in `/workspace/polint-perf-research` on `perf/astra-10x`. Run the GSD quick workflow inline; the user's unattended mission supplies all decisions and gates. Keep exploratory reports and raw evidence in the ignored `.context/perf-research/` directory. Do not change accuracy baselines, CI, or public API boundaries.

## Tasks

1. Establish pinned public corpus identities, licenses, cache configuration, release baseline measurements (one cold and three warm runs per suite), accuracy results, and deep-path profiles. Read prior research before selecting structural changes. Record hypotheses and decisions in `wave1-RESEARCH.md`.
2. Implement and measure the strongest structural improvement, then repeat research on the new profile for a second independent wave. Preserve all diagnostics, precision, recall, unknowns, stable identities, and cache semantics. Add focused equivalence/regression tests for new algorithms. Every retained wave must pass the fixed external accuracy gate and the workspace tests, formatting, and clippy before its commit. Prove byte-identical output on jelly and golang-tools if parallel scheduling changes.
3. Rerun final gates, write the complete before/after report and next-lever analysis, update this task's summary and STATE.md, commit verified work, push only `perf/astra-10x`, and create a draft PR to `main` with title prefix `wip(perf):` and @emilwareus in its body. Never merge.

The September 7 continuation explicitly permits publication of the pure performance work with the inherited Jelly gate failure disclosed if the accuracy restoration cannot pass. The restoration reached F1 0.765784 against the unchanged 0.790193 committed baseline, below the 0.005 tolerance, and is archived outside the retained source. Do not commit that experiment. Run and report the original gates without weakening them; distinguish the inherited failure from any new failures.

## Verification

- `POLINT_REQUIRE_BENCH_CORPUS=1 POLINT_WRITE_GRAPH_BENCH=1 POLINT_GRAPH_BENCH_TIER=release cargo test -p polint --lib --all-features --locked eval::external::tests::external_graph_baseline_reports_can_be_generated -- --nocapture`
- `cargo test --workspace --locked`
- `cargo fmt --all` and `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets` (also all features, locked, and warnings denied when practical)
- Median of three warm runs plus one cold per public suite, peak RSS, pinned SHA and license beside every measurement; syntactic and deep workloads separated.
- `POLINT_CACHE_STORE` unset; check `polint-store-stamp.json` before comparisons; use local-patch depfile proof for any consumer rule pack.

## Completion criteria

Honest measured improvement with unchanged or improved analysis, verified commits, durable reviewable metrics in the draft PR, and `.context/perf-research/FINAL-REPORT.md`. Document rejected ideas, unsupported measurements, and any publication failure explicitly.
